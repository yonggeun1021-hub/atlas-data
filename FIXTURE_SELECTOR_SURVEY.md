# fixture 파일명 선택자 조사 (CIO 지시 2026-08-16)

⛔ **조사만이다. 코드 변경 0줄 · rename FI 미실행 · 수정 미착수 · 권고 없음.**

---

## 0. ★ 앞선 보고를 정정한다 — 위험을 과대평가했다

직전 보고에서 「파일명이 바뀌면 회귀가 조용히 fixture 를 건너뛴다」고 적었다.
**절반만 맞다.**

세 선택자는 전부 **하드코딩된 `TSMC_FX` 6개 목록**을 순회한다.
저장소 어디에도 `glob`·`listdir` 로 fixture 를 **발견**하는 코드가 없다.

```
TSMC_FX = [ (파일명, sha256, NetRevenue행수, Y-o-Y), … ]   # 6개 하드코딩
⑰-0  각 파일 os.path.exists + sha256 대조
```

⇒ **디스크에서 rename 하면 `⑰-0` 의 존재 검사가 즉시 실패한다.**
   내가 우려한 「조용한 누락」은 그 경로로는 일어나지 않는다.

## 1. `_t4_` / `_t6_` 선택자 inventory (질문 ①)

| 위치 | 절 | 순회 대상 | 무엇을 증명하는가 | loop 내 검사 |
|---|---|---|---|---|
| L544 | ⑰-4 | `TSMC_FX` | 무변형 원문에서 P3 값 재현 | 4 × 3개월 = 12 |
| L602 | ⑱-C | `TSMC_FX` | 최종 후보(L1∧L2) == 1 · P3 값 | 4 × 3개월 = 12 |
| L728 | ⑲-1 | `TSMC_FX` | precondition PASS · P3 값 불변 | 3 × 3개월 = 9 |
| L492·L621 | ⑰-2·⑲-2 | 직접 지목 | 천원표(`_t6_`)를 파일명으로 **지목** | — |

선택자 loop 이 만드는 검사 총량 **≈ 33개**.

## 2. 선택 개수를 assert 하는가 (질문 ②) — **하지 않는다**

```
TSMC_FX 선택 개수를 명시 검사하는 줄  0개
```

★ 그래서 **3개월을 기대하면서 2개월만 선택돼도 PASS 한다.**
  `continue` 로 건너뛰므로 실패가 아니라 **검사 수가 줄어든다.**

### 그 경로가 실제로 열리는 조건

| 시나리오 | 결과 |
|---|---|
| 디스크에서 파일 rename | ⑰-0 존재 검사 **실패** → 잡힌다 |
| `TSMC_FX` 목록에서 항목 삭제 | 그 달의 검사가 **조용히 사라진다** ← ⚠️ |
| 새 capture 가 다른 이름 규약으로 산출 (`_t4_` 없음) | 목록 갱신 시 **전부 조용히 사라진다** ← ⚠️ |
| 파일은 있는데 `_t4_` 만 이름에서 빠짐 | ⑰-0 은 실패, 그러나 선택 loop 은 0회 |

⇒ **위험의 실체는 「이름이 사라지면」이 아니라 「목록이나 이름 규약이 바뀌면
   coverage 가 실패 없이 줄어든다」** 이다. 지금은 개수를 아무도 세지 않는다.

★ 이것은 우리가 이미 겪은 계열이다 — 회귀가 **통과했다는 사실만으로** 무엇을
  검증했는지 알 수 없는 구조. `run_all.py` 의 test-set 대조나 FI-4(테스트 누락)가
  파일 단위로는 막지만, **한 파일 안의 loop 이 0회 도는 것**은 막지 못한다.

## 3. 파일명 없이 같은 집합을 정할 수 있는가 (질문 ③) — **가능하다. 두 방법 다.**

실측 (읽기 전용, 구현하지 않음):

| 방법 | 선택 결과 |
|---|---|
| **(가) 내용 identity** — `find_decision_table` 후보가 1건 | **3개** |
| **(나) MANIFEST provenance** — `has_yoy_header ∧ net_revenue_row_count == 1` | **3개** |
| (다) 현재 — 파일명에 `_t4_` | 3개 |

```
세 방식이 같은 집합인가:  True
```

### 두 방법의 성격 차이 (⛔ 어느 쪽이 낫다고 적지 않는다)

| | (가) 내용 identity | (나) MANIFEST provenance |
|---|---|---|
| 근거 | 검증 대상 코드(`find_decision_table`) 자신 | capture 시점에 기록된 관측 사실 |
| 순환 위험 | ⚠️ **있다** — 검사 대상 함수로 검사 입력을 고른다. 그 함수가 망가지면 대상 집합도 함께 비어 조용히 통과할 수 있다 | 없다 — 코드와 독립된 기록이다 |
| 갱신 | 코드가 바뀌면 자동 추종 | capture 를 다시 돌려야 갱신된다 |
| 현재 소비 | — | **소비처 0건** (`tsmc_6k_MANIFEST.json` 을 읽는 코드 없음) |

★ (가)는 **순환 참조 위험**이 있다. `find_decision_table` 이 0건을 내면
  선택 집합도 0이 되어 「검사가 사라지는」 지금 문제와 같은 모양이 된다.
★ (나)는 독립적이지만, 현재 **아무도 읽지 않는 파일**을 회귀 경로에 넣는 결정이다.

⛔ 「MANIFEST 를 써야 한다」는 결론을 미리 정하지 않았다.

## 4. `tsmc_6k_MANIFEST.json` 현황

```
읽는 코드 0곳
담고 있는 것: fixture_file · table_index · slice sha256/offset ·
             unit_declaration · net_revenue_row_count · net_revenue_rows ·
             has_yoy_header · rows/cols · document_sha256 ·
             decision_table_candidates
```

★ 선택에 필요한 정보(③-나)는 **이미 다 들어 있다.**
   (MSFT 쪽 `azure_cc_MANIFEST.json` 은 회귀 C-0 이 무결성 확인에 읽고 있다 —
    같은 성격의 파일인데 한쪽만 소비되고 있다.)

## 5. 판정에 필요한 사실만

1. 파일명 선택자는 **발견이 아니라 분류**다. 디스크 rename 은 sha 검사가 잡는다.
2. 그러나 **선택 개수를 아무도 세지 않는다** — 목록/이름 규약이 바뀌면 33개 검사가
   실패 없이 사라질 수 있다. 이쪽이 실제 결함이다.
3. 파일명 없이 같은 집합을 정하는 방법이 **둘 다 실제로 작동한다**(3/3 일치).
4. (가)는 순환 참조 위험이 있고, (나)는 현재 소비처가 0인 파일을 쓰게 된다.

⛔ 권고를 적지 않는다.

## 6. 상태

```
C4 selection / build_header      CLOSED
P3 · RULE-0003/0007/0008         CLOSED · READY
capture unit metadata            OBSERVABILITY DEBT · 수정 보류

fixture filename selector        OPEN — 조사 완료 · 판정 대기
  └ 실제 결함은 「선택 개수 미검증」이며 파일명 사용 자체가 아니다
TSMC MANIFEST consumer           0건 — 선택에 필요한 정보는 이미 보유

공용 helper inventory            후순위
mutation harness hardening       후순위
```
