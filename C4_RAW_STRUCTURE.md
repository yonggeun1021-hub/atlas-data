# C4 — 실제 TSMC 6-K 원문 구조 확정 (2026-08-16)

artifact `tsmc-raw-fixtures` (run `31926693739` · `b1-p2/c4-firstmatch-fi` @ `a05251a`)
ZIP SHA-256 `f3504987…19cf` — CIO 확인값과 일치.

⛔ **FI 를 하지 않았다.** 원문 구조 확정까지만이다. ⛔ 코드를 수정하지 않았다.
⛔ P3 · `RULE-0003/0007/0008` 상태를 바꾸지 않았다.

---

## ① MANIFEST ↔ HTML — 6/6 일치

3개월 × 표 2건 전부에서 `slice_sha256` · `slice_chars` · `slice_end - slice_start`
가 모두 일치한다. `verbatim_substring_of_document = true`.

| 대상월 | 제출일 | accession | 문서 sha | 결정표 후보 |
|---|---|---|---|---|
| May 2026 | 2026-06-10 | `…26-000367` | `70f9b29d…` (88,046자) | `[4]` |
| June 2026 | 2026-07-13 | `…26-000447` | `dc46a26d…` (88,011자) | `[4]` |
| July 2026 | 2026-08-10 | `…26-000471` | `40ed24df…` (88,661자) | `[4]` |

## ② 단위 선언의 실제 위치 — **표 안에 있다**

```
table[0] row[2] col[7] = '(Unit:NT$ million)'
텍스트   'TSMC July Revenue Report (Consolidated): (Unit:NT$ million) Period July 2026 …'
```

★ 내 capture 도구가 단위를 `None` 으로 기록한 것은 **도구가 표 앞쪽만 뒤졌기 때문**이다.
  문서 결함이 아니고, 슬라이스에는 선언이 온전히 들어 있다.
  ⇒ C4 가 `RE_UNIT_MILLION` 을 **표 텍스트**에 대해 검사하도록 설계한 것이 맞았다.
  ⛔ 도구를 고치지 않았다 (승인 범위 밖).

⚠️ 결과적으로 fixture 파일명에 `unit-unknown` 이 붙었다. **파일명은 hint 일 뿐**이며
  실제 identity 는 슬라이스 내용이 갖고 있다 — 우리가 계속 지켜온 규율 그대로다.

## ③ 무변형 원문에서 기존 P3 값 재현 — **일치**

| 대상월 | 원문 슬라이스에서 추출 | P3 기록값 | |
|---|---|---|---|
| June 2026 | `442,680 / 67.9 / 2,404,484 / 35.6` | 동일 | ★ **일치** |
| July 2026 | `467,580 / 44.7 / 2,872,064 / 37.0` | 동일 | ★ **일치** |
| May 2026 | `416,975 / 30.1 / 1,961,804 / 30.0` | (P3 기록 없음 · 인접 달) | — |

⇒ **정상 원문에서 현재 parser 가 내는 값은 P3 가 기록한 값과 같다.**
   이번 FI 발견이 기존 관측을 무효화하지 않는다는 CIO 판단이 원문으로 확인됐다.

## ④ candidate set — 3개월 모두 정확히 1건

```
`Net Revenue` 를 가진 표      2건
find_decision_table 후보      1건 (table[4], 슬라이스 기준 table[0])
그 표 안 Net Revenue 행       1개 (row 5)
```

★ **실제 문서에서는 표 축·행 축 모두 모호성이 없다.** 후보가 1건이므로 first-match
  가 실행되어도 고를 것이 하나뿐이다.

## ⑤ ★ `Y-o-Y` predicate 가 무엇을 하고 있는가 — 원문으로 확정

두 표의 실제 구조 (2026-08-10 · July 보고서):

```
결정표 t4   (Unit:NT$ million)  · Y-o-Y 있음 · Net Revenue 행 1개
천원표 t6   Period | Items | 2026 | 2025      · Y-o-Y 없음 · Net Revenue 행 2개
   [2] ['Jul.',      'Net Revenue', '467,580,548',   '', '323,165,707']
   [3] ['Jan.~Jul.', 'Net Revenue', '2,872,064,238', '', '2,096,211,240']
```

- **천원표가 `Net Revenue` 행을 2개 갖는다는 것은 합성이 아니라 실재다** — 3개월 모두.
  내가 FI 에서 쓴 구조가 실제 발행인 문서에 그대로 있다.
- 그 표에는 `Y-o-Y` 헤더가 없고, 그 **한 조건**이 후보에서 제외한다.
- 3개월 모두 동일한 구조다 — 이 기간에는 안정적이다.

⇒ **`Y-o-Y` 조건이 현재 유일성을 만들고 있다.** 그리고 그 조건이 없었다면
  후보는 2건이 되고, 그중 하나는 **`Net Revenue` 행이 2개인 표**다.
  즉 first-match 결함이 발현할 재료가 **문서 안에 이미 다 있고**, 지금은 조건
  하나가 그 앞을 막고 있다.

⛔ 다만 이것을 「장애가 임박했다」로 읽지 않는다 (CIO 규율).
   현재 원문에서 그 조건은 정확히 제 역할을 하고 있고, 값도 P3 와 같다.

## ⑥ 상태 — 변경 없음

```
RULE-0003 · 0007 · 0008        P3 CLOSED 유지 · AVAILABLE / SOURCE_RESOLVED / READY
c4_sec_edgar_check             코드 변경 0줄
C4 row/table first-match       CONFIRMED LATENT CORRECTNESS DEFECT (등록 상태 유지)
FI                             ⛔ 실행하지 않았다 — 범위 판정 대기
```

## ⑦ 판정을 기다리는 것

1. 실제 markup 최소변형 FI 의 **범위** — 특히 `Y-o-Y` 를 흔드는 변형을 허용할지
2. 그 FI 로 재현되면 C4 local fail-closed 수정안 설계 착수 여부
3. capture 도구의 단위 탐색 범위(표 앞만 봄)를 고칠지 — ⛔ 지금은 그대로 두었다

⛔ 권고를 적지 않는다. 위 셋 모두 CIO 판정 사항이다.
