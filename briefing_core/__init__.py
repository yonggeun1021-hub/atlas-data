"""Versioned Atlas briefing-core contracts.

The package is intentionally separate from ``briefing.daily_orchestrator``.
Import concrete contracts from :mod:`briefing_core.chain` so the command-line
entry point can execute without importing itself through this package first.
"""
