# 유예 항목 — 지금 고치지 않기로 CIO 가 판정한 건

이 파일은 artifact 가 아니다. 해시 체인에 들어가지 않고 evaluator 도 읽지 않는다.
잊지 않기 위한 기록일 뿐이며, 여기 적힌 것을 임의로 실행하지 않는다.

| 항목 | 상태 |
|---|---|
| D-1 카드 1·2·3 의 evidence 시제 | ✅ CLOSED — 수정하지 않음 |
| D-2 카드 5·6 의 선행조건 상태 전이 | ✅ 해소 |
| D-3 `RULE-0025` 「끊김」 의 기계적 의미 | ✅ 해소 |
| **D-4 숫자 토큰 정의가 두 갈래다** | **미해소** |
| **D-5 validator 오류 문자열 결합** | **미해소** |
| **D-6 실행 중 모듈 전역 재바인딩 감시 한계** | **미해소** |
| **D-7 mutable global mutation-surface 일반화** | **미해소** |

---

## D-7 · mutable global mutation-surface 일반화 (2026-08-15 접수) — 미해소

**증상**
`_Tripwire._MUTATORS` 는 하드코딩된 메서드 이름 목록이다. 타입에서 변형 API 를
유도하는 구조가 아니다.

**현재 상태 — 결함 아님**
현재 쓰이는 mutable 전역 타입(dict · list · set · bytearray)의 실제 변형 경로는
회귀와 실측에서 **모두 차단됨**을 확인했다. `bytearray` 는 mutable sequence 라
메서드 이름이 list 와 겹쳐 결과적으로 덮여 있다.
★ 다만 이는 우연히 충분한 것이지 설계된 일반화가 아니다.

**향후 위험**
새로운 mutable 타입이 전역에 추가되면 그 타입 고유의 변형 API 가 감시에서
누락될 수 있다 (예: `deque.rotate` · `appendleft`, `array.byteswap`).

**CIO 판정**
D-7 을 이유로 현재 구현을 변경하지 않는다. B5-2B 를 다시 열지 않는다.

**해소 시점 / 방법**
새 mutable 타입이 전역 상태에 들어올 때 해소한다. 그때 타입별 mutation surface 를
명시적으로 유도하고, **해당 타입의 실제 변형 API 에 대한 음성 커버리지 테스트**를
함께 추가한다.

**해소 시 함께 넣을 회귀**
- 감시 대상 타입별로 변형 API 전수를 시도해 모두 차단되는가 (음성)
- 목록에 이름만 있고 실제로는 감싸지지 않는 항목이 없는가
- 새 타입이 추가됐는데 감시 대상에서 빠지면 잡히는가

---

## D-6 · `build()` 실행 중 모듈 전역 재바인딩 감시 한계 (2026-08-15 접수) — 미해소

**증상**
K-15 는 mutable 전역의 실행 중 in-place 변형과, build 전후의 내용 · 객체 동일성을
검증한다. 다만 `build()` 가 실행 도중 모듈 전역 **이름 자체**를 다른 객체에
잠시 재바인딩했다가 원복하는 경로는 탐지하지 못한다.
전후 비교는 원복된 뒤를 보고, tripwire 는 객체 변형 감시이지 모듈 속성 대입
감시가 아니기 때문이다.

**현재 상태**
관측된 결함이 아니라 **회귀 감시 범위의 잔여 한계**로 기록한다.
현재 `build()` 가 그런 경로를 쓴다는 증거는 없다.

**CIO 판정**
지금 해소하지 않는다. B5-2B 완료를 막지 않으며 production 도 테스트도 바꾸지 않는다.

**해소 시점 / 방법**
실제 재바인딩 경로가 발견되거나, build purity 계약을 더 강화하기로 별도 판정할 때
해소한다.
★ 모듈 `__setattr__` 후킹 하나를 급히 붙이지 않는다. 모듈 내부에서 `global` 문이나
globals 딕셔너리로 직접 바꾸는 경로에는 그 후킹이 걸리지 않으므로, **감시하고
있다는 잘못된 안전감**만 만든다. 별도 설계 대상으로 남긴다.

**해소 시 함께 넣을 회귀**
- 실행 중 재바인딩을 실제로 주입했을 때 잡히는가 (음성)
- 후킹이 닿지 않는 경로가 무엇인지 문서화돼 있는가
- 기존 K-15 검사 결과가 바뀌지 않는가

---

## D-5 · validator 오류 문자열 결합 제거 (2026-08-15 접수) — 미해소

**증상**
음성 테스트 일부가 validator 의 오류 메시지 문자열을 판정 계약으로 쓴다.
`"간접 충족으로 새는 경로"` · `"판정된 의존 형태와 다르다"` · `"내가 대신 결정한 것이다"`
같은 문구가 사실상 API 가 돼 있다.

**실제 위험**
- 기능이 같아도 문구만 다듬으면 회귀가 깨진다.
- ★ 더 나쁜 쪽 — validator 와 테스트 문구를 **함께** 바꾸면 음성 테스트가 조용히
  다른 것을 검사하게 된다. 실패하지 않으므로 드러나지도 않는다.

**CIO 판정**
지금 구현하지 않는다. 전부 통과하는 상태에서 광범위한 음성 테스트 계약을
건드리는 것은 얻는 것보다 회귀 위험이 크다. B5-2B 정본을 그대로 둔다.

**해소 시점 / 방법**
★ 부분 치환하지 않는다. validator 오류 코드 체계를 **한 번에** 도입하고,
기존 음성 fixture 각각에 대해 **변경 전 탐지 의미 ↔ 변경 후 오류 코드**를 대조해
같은 위반을 잡는지 확인한 뒤에야 문자열 의존을 제거한다.
⛔ bespoke runner 이관과 묶지 않는다. 두 변경을 섞으면 무엇이 회귀를 깼는지 못 가린다.

**해소 시 함께 넣을 회귀**
- 오류 코드가 닫힌 집합인가
- 각 음성 fixture 가 코드 기준으로도 같은 위반을 잡는가
- 메시지 문구를 바꿔도 회귀가 깨지지 않는가

---

## D-4 · 숫자 토큰 정의가 두 갈래다 (2026-08-15 접수) — 미해소

**증상**
`decision_cards.py` 안에서 `re.findall(r"\d+", …)` 와 `re.search(r"\d", …)` 가 혼재한다.
파이썬의 유니코드 문자열 정규식에서 `\d` 는 ASCII `0-9` 만이 아니라 유니코드
십진 숫자(Nd) 전체를 매칭한다. 지금 가드가 뜻하는 '숫자 토큰' 은 ASCII 를
전제하고 있으므로, 두 갈래가 어긋날 수 있다.

**실제 위험**
- 판정문에 유니코드 십진 숫자가 들어오면 스캔은 잡지만 선언 토큰과 문자열이 달라,
  영영 통과하지 못하거나 반대로 의도치 않게 통과할 수 있다.
- 즉 '선언되지 않은 숫자' 판정의 의미가 입력 문자 집합에 따라 달라진다.

**CIO 판정**
지금 고치지 않는다. 현재 입력은 전부 ASCII 이고 이 단계의 입력 계약을 깨뜨린
증거가 없다. 부분적인 정규식 치환으로 처리할 문제도 아니다.

**해소 시점 / 방법**
★ 정규식을 몇 군데 바꾸는 방식으로 처리하지 않는다.
숫자 토큰의 정의를 **하나의 함수 · 정책으로 통일**하고 모든 호출부가 그것만 쓰게 한다.
ASCII 로 제한할지 여부도 그때 함께 판정한다.

**해소 시 함께 넣을 회귀**
- 유니코드 십진 숫자를 판정문에 주입했을 때 정의대로 동작하는가 (음성)
- 스캔 함수와 선언 대조가 같은 숫자 정의를 쓰는가
- 기존 ASCII 입력의 판정 결과가 바뀌지 않는가

**진행 상황 (2026-08-15)**
회귀 쪽은 먼저 정리했다. 테스트가 자체 숫자 정규식을 쓰지 않고 production
`scan_numeric_tokens()` 만 쓰도록 통일했고, 회귀 파일이 스스로 `\d` 정규식을
쓰지 않는지 검사한다. 따라서 테스트와 validator 사이의 specification drift 는
닫혔다. ★ 남은 것은 production lexer 자체의 유니코드 정책이다.
현재 관측된 동작: `"전각 １２ 와 ASCII 12"` → `['１２', '12']`,
`"١٢٣"` → `['١٢٣']`. 즉 유니코드 십진 숫자가 토큰으로 잡히며, 선언 토큰과
문자열이 정확히 일치해야만 통과한다. 이 동작을 유지할지 ASCII 로 제한할지가
CIO 판정 대상이다.

★ self-check 의 한계 (CIO 지적, 2026-08-15)
회귀가 자체 숫자 정규식을 쓰지 않는지 보는 검사는 **대표적인 재발 경로 하나**만
막는다. 작은따옴표 패턴 · `re.compile()` · 변수에 담아 넘기는 방식은 통과할 수
있다. ⛔ 지금 AST 검사 등으로 확대하지 않는다 — 그것 자체가 테스트 인프라 변경이
된다. production lexer 정책을 판정할 때 함께 다룬다.

---

## D-3 · `RULE-0025` 「끊김」 의 기계적 의미 — ✅ 해소 (재고정 단계)

**최초 증상**
기간은 닫혔으나 무엇을 한 번의 「끊김」 관측으로 볼지가 어느 artifact 에도 없었다.
B5-0 이 이 규칙의 결핍을 `time_window` 하나로만 잡았기 때문이다.

**원인**
원문 주석이 「연속」 만 미정의로 선언했고, B5-0 이 그 자기 선언을 결핍 목록으로
그대로 채택했다. 자기 선언은 작성자가 알아챈 것이지 빠짐없는 목록이 아니다.

**해소 방식**
CIO 가 B5-0 누락으로 확정했다. `event_definition` 결핍을 인벤토리에 반영했고,
그 위에 원문 구조 미결(「연속」 의 귀속)이 **별도 축**으로 올라왔다.
정의 성분 어휘(`allowed_missing_components`)에는 넣지 않았다 — 층이 다르다.
카드 33 → 35, 질문 40 → 42. 기존 기간 판정은 값을 보존한 채 집행 보류로 내렸고,
두 선행이 모두 판정된 뒤 보류가 해제됐다.

**남은 것** 없음. 세 단위 모두 CIO 판정 완료.

---

## D-2 · 카드 5·6 의 `blocked_by` 상태 전이 — ✅ 해소 (카드 5 판정 단계)

해소 방식: `blocked_by` 는 현재 상태만 담고, `prerequisite` 가 이력을 담는다.
`{unit, reason, status, satisfied_by, history}` — 무엇에 막혀 있었는지(`history`)와
무엇이 풀었는지(`satisfied_by`)가 둘 다 남는다. 삭제하지 않았다.
`status` 는 선행 카드의 판정 유무에서 유도한다 — 손으로 적지 않는다.
회귀: 선행 판정을 지우면 다시 `대기` 로 돌아가고 대기 표시도 복원되는지 확인한다.

---

## D-1 · 카드 1·2·3 의 `observable_evidence` 시제 — ✅ CLOSED (수정 없음)

**최초 증상**
판정 전 상태의 현재형 문장이 남아 있었다.

> "원문에 측정 대상이 없어, 대상이 정해지기 전에는 수집 가능성도 판정할 수 없다."

**재검사 결과**
provenance 정상. 두 evidence 모두 상위 artifact 로 추적되고, 두 artifact 다 카드
생성보다 앞서 확정됐으며 해시가 불변이다. 판정 이후에만 알 수 있는 어휘는 0건 —
후행 정보 역류 없음.

★ 원래 범위보다 넓었다. 시제가 낡은 카드는 2·3 이 아니라 **1·2·3 세 장**이고,
evidence 는 카드별이 아니라 **규칙 집합 단위 블록**으로 공유된다(13/13 그룹이 단일 블록).

**CIO 판정**
판정 결함이 아니므로 수정하지 않는다. `historical / current` 분리는 구조 개선
후보로만 남기고 이 체인에서 손대지 않는다.
