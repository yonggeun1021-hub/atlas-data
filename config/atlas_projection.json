{
  "_comment": "What a post-delivery source change must PROVE before it counts as finished. A signed CIO ruling establishes WHETHER the change moves an investment conclusion; it does not establish that the record was corrected or that anyone was told. Those need receipts from real adapters.",
  "portal": {
    "adapter": "notion_cockpit",
    "implemented": true,
    "verified_against_live_api": true,
    "api_version": "2026-03-11",
    "data_source_id": "7207d3d2-8806-4aa9-bfe2-cfddc3ea6869",
    "database_id": "2ede08c3-1288-47fc-a8e4-0fb63713fa83",
    "upsert_identity": ["briefing_id", "post_delivery_change_key"],
    "canonical_hash": "SHA256(sorted-key compact UTF-8 JSON)",
    "read_after_write_required": true,
    "receipt_publication": "ATOMIC_APPEND_ONLY",
    "receipt_authority": "LATEST_REVISION_PER_CHANGE",
    "normal_delivery_order": "PORTAL_PROJECTION_BEFORE_SINGLE_USER_DELIVERY",
    "post_delivery_redelivery": "FORBIDDEN",
    "_note": "Activated after GitHub Actions canary run 33132227994 verified the reviewed schema, exact canonical readback, NO_CHANGE replay, receipt reuse, and append-only receipt commit bb59b8a for 2026-08-28-am. Normal projection is now a fail-closed pre-delivery gate."
  },
  "alert": {
    "required_for_capital_impact": ["PRESENT"],
    "user_reaching_channels": [],
    "_note": "github_step_summary is NOT user-reaching: it requires a person to open Actions. Listing a channel here asserts that a message on it actually arrives at the user. Empty until a FULL delivery adapter is verified live."
  }
}
