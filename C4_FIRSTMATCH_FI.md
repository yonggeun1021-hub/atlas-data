# C4 first-match — Fault Injection 증거 (2026-08-16)

⛔ **`c4_sec_edgar_check` 를 수정하지 않았다.** 코드 변경 0줄.
⛔ P3 / RULE-0003·0007·0008 을 건드리지 않았다. ⛔ 수정안을 올리지 않는다.
⛔ `build_header` 와 섞지 않았다 — 이번 Gate 는 row/table **선택**만 본다.

---

## 1. 결론

| 축 | 재현 | 판정 |
|---|---|---|
| **표 first-match** (main 의 `break`) | ✅ **SILENT WRONG 재현** | 순서가 값을 정한다 |
| **행 first-match** (`find_decision_table` 의 `break`) | ✅ **SILENT WRONG 재현** | 순서가 값을 정한다 |
| 행 모호성 + 결합 실패 조합 | ⚠️ fail-closed | 항상 silent 는 아니다 |
| `Y-o-Y` 추가 조건 (⑤) | ✅ **실제로 후보를 줄인다** | 그러나 ambiguity 를 **제거하지는 못한다** |

★ MSFT 에서 발견한 것과 **같은 결함이 C4 에도 실재**한다.
⚠️ 다만 **증거의 급이 다르다** — 4절을 반드시 함께 읽어야 한다.

## 2. ⑤ `Y-o-Y` 조건은 load-bearing 이다 — 그러나 충분하지 않다

C4 의 `find_decision_table` 은 `Net Revenue` 행에 더해 **`Y-o-Y` 헤더 존재**를 요구한다.
이것이 실제로 일을 한다:

```
문서 내 table 2개
  table[0]  NT$ million 표    · 'Net Revenue' 셀 1개 · Y-o-Y 헤더 True
  table[1]  NT$ thousands 표  · 'Net Revenue' 셀 2개 · Y-o-Y 헤더 False   ← 걸러짐

'Net Revenue' 를 가진 표 2건 → Y-o-Y 조건 통과 1건
```

★ **NT$ thousands 표는 `Net Revenue` 행을 실제로 2개 갖는다** (월 행 + 누계 행).
  즉 **행 모호성 자체는 TSMC 문서 구조에 원래 존재**하며, 지금은 `Y-o-Y` 조건이
  그 표를 후보에서 빼기 때문에 드러나지 않는다.

⇒ ambiguity 는 **제거된 것이 아니라 가려져 있다.** 조건 하나에 의존하고 있다.

## 3. 재현 증거

### FI-A — 표 first-match (자격 표 2건, 순서만 교환)

```
위조 표가 앞   후보 2건 · table[0] · ('111,111', '99.9', '222,222', '88.8')
위조 표가 뒤   후보 2건 · table[0] · ('467,580', '44.7', '2,872,064', '37.0')

⇒ ★★ SILENT WRONG — 두 경우 모두 후보가 2건인데 거부하지 않고 첫 표를 쓴다.
```

### FI-B2 — 행 first-match (같은 표 안 결합 가능한 `Net Revenue` 행 2개)

```
위조 행이 앞   후보표 1건 · di=1 · ('111,111', '99.9', '222,222', '88.8')
위조 행이 뒤   후보표 1건 · di=1 · ('467,580', '44.7', '2,872,064', '37.0')

⇒ ★★ SILENT WRONG — 행 순서가 값을 정한다.
```

### FI-B — 행 모호성이 있으나 결합에 실패하는 경우

NT$ thousands 표에 `Y-o-Y` 를 부여해 유일 후보로 만들면:

```
table[0] Net Revenue 행 [1, 2] → find_decision_table 이 고른 di=1
  행[1] ['Jul.',      'Net Revenue', '467,580,548',   '323,165,707']
  행[2] ['Jan.~Jul.', 'Net Revenue', '2,872,064,238', '2,096,211,240']  ← 조용히 버려졌다
  결합 → FAIL-CLOSED ['대상월 헤더가 정확히 1개가 아니다 (0개)', …]
```

★ **행 선택은 조용하지만**(거부 없이 첫 행을 쓰고 둘째를 버린다),
  그 뒤 `bind_columns` 가 막아서 이 경우엔 값이 나오지 않았다.
⇒ 행 first-match 가 **항상** silent wrong 이 되는 것은 아니다.
   두 행이 **모두 결합 가능할 때**만 그렇게 된다 (FI-B2).

## 4. ★★ 증거의 한계 — MSFT 와 같은 급으로 취급하면 안 된다

| | MSFT (RULE-0021) | **C4 (이번)** |
|---|---|---|
| 기반 문서 | **실제 SEC 원문 슬라이스 4건** (artifact 보존 · sha256 고정) | **합성 문서** (C4 회귀의 `doc()` 빌더) |
| 변형 방식 | 실제 markup 최소변형 + 일부 합성 | **합성 위에 합성** |
| 실제 관측 여부 | 모호성 관측된 적 없음 (표 1건 · 행 1건) | **미확인 — 보존된 원문 증거가 없다** |

⛔ **C4 에는 보존된 실제 마크업 fixture 가 없다.** 남아 있는 실측 증거는
   2026-08-15 live run 로그에서 가져온 **header/data 행렬 한 벌**뿐이며,
   그것은 표가 몇 개인지·`Net Revenue` 행이 몇 개인지를 담지 않는다.

⇒ 따라서 이번 재현이 말해 주는 것은 **「코드가 그런 입력에서 그렇게 동작한다」**이지
  **「실제 TSMC 6-K 가 그런 구조다」가 아니다.**
  ⛔ 「코드 모양이 MSFT 와 같다」에서 한 걸음 나아갔지만, 실제 발행인 구조에 대한
     증거는 여전히 없다. 이 구분을 흐리지 않는다.

★ 부분적 예외 하나 — **NT$ thousands 표의 `Net Revenue` 행 2개는 합성이 아니라
  C4 자신의 문서 모델이 재현하는 구조**다. 즉 행 모호성의 소재는 실재에 가깝다.
  다만 그 표가 결정표 후보가 되는 상황은 합성이다.

## 5. 판정에 필요한 사실만

1. 두 축 모두 **silent wrong 이 코드 수준에서 재현된다.**
2. `Y-o-Y` 조건은 후보를 줄이지만 ambiguity 를 **제거하지 못한다.**
3. 실제 TSMC 6-K 가 모호한 구조를 갖는지는 **미확인**이다 — 원문 fixture 부재.
4. 원문 확보 수단은 이미 존재한다 — `capture_azure_fixture.py` 와 같은 방식의
   bounded 원문 보존. ⛔ 이번에 만들지 않았고 제안으로만 적는다.

⛔ P3 reopen 여부 · B(common contract) 검토 여부에 대한 권고를 적지 않는다.
   CIO 판정 사항이다.

## 6. 상태

```
RULE-0003 · 0007 · 0008        변경 없음 (P3 CLOSED 유지)
c4_sec_edgar_check             코드 변경 0줄
C4 row/table first-match       silent-wrong 재현됨 · 실제 구조 미확인
RULE-0021                      이번 Gate 에서 건드리지 않음
```
