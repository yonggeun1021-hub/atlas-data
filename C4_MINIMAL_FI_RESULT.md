# C4 최소변형 FI 결과 — silent wrong **재현되지 않음** (2026-08-16)

실제 fixture 3개월(`tsmc-raw-fixtures` · sha256 고정) 기반.
⛔ 표 복제·합성 없음 — **셀 문면 1곳**만 바꿨다. ⛔ 코드 수정 없음. ⛔ P3 불변.

---

## 1. 결론 — 승인 범위 안에서는 막힌다

| 축 | 결과 |
|---|---|
| `Y-o-Y` 최소변형으로 천원표가 decision predicate 통과 | ✅ **통과한다** (후보 0건 → 1건) |
| 그 표 안 `Net Revenue` 행 first-match | ✅ **재현 — 첫 행 선택, 둘째 행 조용히 버림** |
| 그 결과가 **틀린 값**이 되는가 | ❌ **아니다 — fail-closed** |
| 표 후보 2건일 때 순서가 값을 바꾸는가 | ❌ **아니다 — 순서 무관하게 P3 값** |

⇒ **silent wrong 재현 실패.** 정확성 결함으로 승격할 근거를 이번 FI 는 만들지 못했다.

## 2. 최소변형의 내용

천원표 헤더 행의 마지막 컬럼 라벨 셀 **1곳**을 million 표와 같은 문면으로 바꿨다.

```
before  ... <font ...>2025</font>
after   ... <font ...>2025 Y-o-Y Increase (Decrease) %</font>
```

⛔ 표를 복제하지 않았다. 새 표를 만들지 않았다. 행을 추가하지 않았다.

## 3. 관측 결과 (June · July 동일)

```
[A] 천원표 단독 · 무변형     find_decision_table 후보 0건
[B] 천원표 단독 · 최소변형   후보 1건
        Net Revenue 행 [2, 3] → di=2   ★ 첫 행 선택 · 행[3] 조용히 버림
        결합 → FAIL-CLOSED

[C] 같은 filing 의 두 실제 표를 한 문서로 (순서만 교환)
    결정표 먼저   후보 2건 · table[0] 결합 성공 → 442,680 / 67.9 / 2,404,484 / 35.6
    천원표 먼저   후보 2건 · table[0] FAIL-CLOSED → table[1] 결합 성공 → 동일 값
    → 두 배치 모두 P3 값과 일치. 순서가 값을 바꾸지 못했다.
```

★ **행 first-match 자체는 재현됐다** — 후보 행이 2개인데 거부하지 않고 첫 행을 쓰고
  둘째를 버린다. 그러나 그 선택이 **틀린 값으로 이어지지 않았다.**

## 4. ★ 왜 막혔는가 — 두 번째 identity 층이 잡는다

```
천원표 헤더   ['Period', 'Items', '2026', '2025 Y-o-Y Increase (Decrease) %', '']

bind_columns 실패 사유 5건
  · 대상월 헤더가 정확히 1개가 아니다 (0개)
  · 전년동월 헤더가 정확히 1개가 아니다 (0개)
  · 당해누계 헤더가 정확히 1개가 아니다 (0개)
  · 전년누계 헤더가 정확히 1개가 아니다 (0개)
  · Y-o-Y 헤더가 2개가 아니다 (1개)
```

★ **천원표는 컬럼 identity 가 근본적으로 다르다.** 기간·전년동월·누계 헤더가 아예 없다.
  `Y-o-Y` 하나를 통과해도 **`bind_columns` 의 「정확히 1개」 가드가 4중으로 막는다.**

⇒ C4 에는 identity 층이 두 겹 있고, first-match 는 **첫째 층에만** 있다.
   둘째 층(컬럼 identity)이 실질적 방어를 하고 있다.

### 이걸 통과시키려면 무엇이 필요한가

누락 헤더 **4종**을 채워야 한다 = 컬럼 identity 를 새로 구성 = **표 합성**.
⛔ 승인 범위(복제·합성 제외) 밖이므로 하지 않았다.

## 5. ★ 앞선 보고를 정정한다

`e44dc1b` 의 합성 FI 에서 두 축 모두 silent wrong 을 재현했다. 그때 쓴 것은
**결정표를 복제해 값만 바꾼 표**였고, 그래서 컬럼 identity 가 이미 올바랐다.

⇒ 그 재현은 **「컬럼 identity 가 같은 표가 둘일 때」** 를 보인 것이지,
  **실제 문서에 있는 두 표 사이에서 일어날 수 있음**을 보인 것이 아니다.
  실제 두 표는 컬럼 identity 가 달라서 서로를 대체할 수 없다.

★ 내 앞선 보고가 도달 가능성을 과대평가했다. 그대로 정정한다.

## 6. 남는 사실 — 축소하지도 않는다

- `find_decision_table` 이 후보 행 2개에서 **거부 없이 첫 행을 쓰는 동작은 실재**하고,
  그 상황은 실제 문서(천원표)에 이미 존재한다.
- 지금 이것이 무해한 이유는 **그 표가 컬럼 identity 에서 걸리기 때문**이지,
  선택 로직이 안전해서가 아니다.
- 즉 방어는 **우연에 가까운 이중 방어**이고, 설계된 유일성 판정이 아니다.

## 7. 상태 — 변경 없음

```
RULE-0003 · 0007 · 0008     P3 CLOSED 유지 · AVAILABLE / SOURCE_RESOLVED / READY
c4_sec_edgar_check          코드 변경 0줄
C4 first-match              실제 markup 최소변형에서 silent wrong **미재현**
                            (행 first-match 동작 자체는 재현)
```

## 8. 판정을 기다리는 것

CIO 판정 기준 「재현되지 않으면 왜 막혔는지부터 다시 판정한다」에 해당한다.
사실만 올린다 — ⛔ 권고를 적지 않는다.

1. 이 결과로 결함 등급을 재분류할지 (`CONFIRMED LATENT` → 유지 / 강등 / 다른 분류)
2. C4-local fail-closed 설계를 그래도 착수할지 — 근거는 「값이 틀렸다」가 아니라
   「유일성 판정이 선택 층에 없다」가 된다
3. 범위를 넓힌 FI(컬럼 identity 까지 맞춘 합성)를 별도 승인할지
