# B5-2C · D-3 / D-1 재검사 보고

> ⛔ 이 문서는 보고서다. 정의를 만들지 않았고 판정을 바꾸지 않았다.
> `rules.json` 미생성 · canonical 미반영 · evaluator 미연결 ·
> `authority=false` · `consumable_by_evaluator=false` 유지.

---

## ① D-3 — `RULE-0025` 「끊김」 : **B5-0 누락으로 확인됨**

### 무엇을 봤는가

원문 조각(`298040.KS::탈락 조건`)은 다음과 같다.

| # | 조각 | 판정 |
|---|---|---|
| 5 | 「기관 순매수 연속 끊김」 | `rule_candidate` / `FAL` / `UNDEFINED` / data `AVAILABLE` |
| 6 | 「⚠ '연속'의 정의가 Undefined(8/15 Review #2 이월). 정의 전까지 이 조건으로 탈락 판정하지 않는다.」 | `non_rule_evidence`, #5 를 주석 |

B5-0 은 이 규칙의 결핍을 `time_window` 하나로만 잡았다.

### 근거 1 — 인벤토리 자신의 기준과 어긋난다

같은 인벤토리에서 **상태 전이 술어**를 가진 조건은 전부 `event_definition` 을 받았다.

| 규칙 | 조건 | 술어 | `event_definition` |
|---|---|---|---|
| RULE-0019 | SK 대비 상대강도 지속 열위 | 「열위」 | ✅ ("'열위' 기준이 없다") |
| RULE-0020 | HBM 공급 확대 미확인 | 「미확인」 | ✅ ("'미확인' 의 판정법이 없다") |
| RULE-0017 | HBM 예약 취소·LTA 축소 | 「취소」 | ✅ |
| RULE-0009 | B 박스권 돌파 후 재확인 | 「돌파」 「재확인」 | ✅ |
| **RULE-0025** | **기관 순매수 연속 끊김** | **「끊김」** | ❌ **없음** |

「끊김」 은 「열위」·「미확인」 과 같은 종류의 술어다. 무엇이 한 번의 「끊김」 관측인지
정하지 않으면 기계 판정이 되지 않는다. 최소한 다음이 원문에도 어느 artifact 에도 없다.

- 어느 투자자 분류를 「기관」 으로 볼 것인가
- 순매수가 **0** 인 날은 끊김인가 아닌가 (카드 14 는 이 물음을 **배제 대상으로 인용만** 했다)
- 순매도 전환만 끊김인가, 순매수 부재도 끊김인가

### 근거 2 — 카드 14 판정문이 존재하지 않는 체계를 참조한다

> "기관 순매수 연속 상태가 끊긴 것으로 **기존 규칙 체계에서 판정되는** 관측이 …"

그 「기존 규칙 체계」 가 없다. 기간은 닫혔으나 세는 대상이 정의돼 있지 않다.
★ 이는 CIO 판정의 결함이 아니라, 판정이 딛고 선 인벤토리의 결함이다.

### 근거 3 — 누락의 원인 (재발 가능성)

B5-0 이 이 건에서 **원문의 자기 선언(#6)을 결핍 목록으로 그대로 채택**했다.
#6 은 작성자가 알아챈 것을 적은 것이지 빠짐없는 목록이 아니다.
다른 자기 선언 건(RULE-0009)은 3개 성분을 받았으므로 체계적 오류는 아니고
이 한 건의 범위 축소로 보인다. 다만 같은 실수가 가능한 경로다.

### ★ 부수 발견 — 「연속」 이 무엇에 걸리는지가 갈린다

| 읽기 | 해석 | 발동 |
|---|---|---|
| A | 「연속 순매수」 가 끊김 | 순매수가 이어지던 상태가 끝날 때 |
| B | 「끊김」 이 연속 | 끊김 관측이 이어질 때 (카드 14 가 채택) |

두 읽기는 서로 다른 규칙이다. B5-0 의 why("'연속' 의 길이가 없다")와
카드 질문("'연속'을 몇 거래일로 정의할 것인가?")은 둘 다 중립이라
어느 쪽인지 상위 artifact 어디에도 고정돼 있지 않다.
⛔ 여기서 고르지 않는다. CIO 판정 대상이다.

### 올려 드리는 CIO decision unit 후보

```
RULE-0025::event_definition
  질문: 무엇을 한 번의 「끊김」 관측으로 볼 것인가?
        (기관 분류 · 순매수 0 의 취급 · 순매도 전환 여부)
  선행: 없음
  후행: RULE-0025::time_window 가 세는 대상이 이 결정으로 정해진다

RULE-0025::time_window (기판정, 카드 14)
  ⚠ 위 「연속」 귀속 읽기(A/B)에 따라 기존 판정의 의미가 달라질 수 있다.
     ⛔ 임의로 재해석하지 않았다. CIO 확인 대상으로만 표시한다.
```

### upstream 영향 범위

`definition_inventory.json` 의 `missing_components` 를 고치면 그 해시가 바뀌고,
아래가 전부 재검증 대상이 된다 (해시 고정 체인).

```
definition_inventory.json  (eb292751…)
   ├── definition_decision.json      pins inventory
   │      └── decision_normalization.json  pins decision
   │             └── decision_cards.json   pins normalization
   └── data_source_ambiguity.json    pins inventory
          └── decision_normalization.json  pins ambiguity
                 └── decision_cards.json   pins ambiguity
```

즉 **B5-0 → B5-1 → B5-1A → B5-2A → B5-2B 5단계 재고정**이 필요하다.
카드 수 33 → 34, 질문 40 → 41. 기존 14건 판정값 자체는 영향받지 않으나
재빌드 후 해시는 전부 새로 발급된다.
⛔ 이 재고정은 착수하지 않았다. CIO 지시 대상이다.

---

## ② D-1 — 카드 2·3 evidence : **provenance 정상, 시제만 낡음**

### 검증 1 — 후행 정보를 소급해 붙였는가 → 아니다

카드 1~3 이 공유하는 evidence 두 줄은 모두 상위 artifact 로 추적된다.

| evidence | 출처 |
|---|---|
| 「설비투자 수치는 공시 본문에 있고 현재 파싱하지 않는다.」 | B5-0 why + B5-1A 분류 |
| 「원문에 측정 대상이 없어, 대상이 정해지기 전에는 수집 가능성도 판정할 수 없다.」 | B5-1A why 「대상이 정해지기 전에는 **판정할 수 없다**」 |

두 artifact 모두 카드 생성보다 앞서 확정됐고 해시가 지금도 불변이다.
그리고 판정 이후에만 알 수 있는 어휘(전사 자본예산 · 가이던스 · 범위 하단 등)는
evidence 에 **0건**이다. → 후행 정보 역류 없음.

### 검증 2 — 시제는 실제로 낡았다. 다만 카드 2·3 만이 아니다

「대상이 정해지기 전에는」 이 남아 있는 카드는 **1·2·3 세 장**이다.
카드 1 은 그 판정이 실린 카드 자신이므로 더 눈에 띈다.

### 검증 3 — 원인은 카드가 아니라 구조다

evidence 는 카드별이 아니라 **규칙 집합 단위 블록**으로 공유된다(13/13 그룹이 단일 블록).
따라서 카드 2·3 만 손보는 방식으로는 고칠 수 없고, 고쳐도 같은 문제가 재발한다.

### 결론

D-1 은 **판정 결함이 아니다.** 판정을 바꿀 이유가 없다.
남는 것은 표기 문제이며, 해소하려면 evidence 를
`historical`(판정 당시 관측, 보존) / `current`(현재 상태, 갱신)로 나누는
구조 변경이 필요하다. ⛔ 이번에 착수하지 않았다.
★ 원래 D-1 범위(카드 2·3)보다 넓다 — 카드 1 포함, 그리고 규칙 단위 블록 전체가 대상이다.

---

## ③ 잔여 decision units — D-3 확정 전까지 확정 불가

현재 미판정 19장은 8개 규칙에 걸쳐 있다.

| 시작 카드 | 규칙 | 미판정 | 성분 |
|---|---|---|---|
| 15 | RULE-0021 | 1 | event_definition |
| 16 | RULE-0022 | 3 | threshold · time_window · comparison_baseline |
| 19 | RULE-0011 | 1 | observation_frequency |
| 20 | RULE-0017 | 3 | event_definition · threshold · data_source |
| 23 | RULE-0020 | 3 | event_definition · threshold · data_source |
| 26 | RULE-0012 | 3 | event_definition · threshold · time_window |
| 29 | RULE-0009 | 3 | event_definition · threshold · time_window |
| 32 | RULE-0016 | 2 | time_window · threshold |

**의존성**: 19장 중 D-3 에 직접 걸리는 것은 없다(모두 다른 규칙).
그러나 D-3 을 반영하면 카드 그래프 전체가 재발급되므로
지금 판정한 값은 재고정 후 다시 실어야 한다.

★ 그래서 **19장 판정보다 D-3 처리가 먼저**라는 순서는 맞다.

**참고**: 미판정 5개 규칙(0021 · 0017 · 0020 · 0012 · 0009)이 `event_definition` 을
가지고 있다. RULE-0025 에 이것이 없다는 점이 더 두드러진다.
