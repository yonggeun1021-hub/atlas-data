# RULE-0022 Definition Gate 조사 + READY provenance audit (CIO 지시 2026-08-16)

⛔ **조사만이다. 코드 변경 0줄 · Rule 상태 변경 0건 · 정의 신설 0건 · 임계 신설 0건.**
⛔ `condition_semantics/scope 25/25 UNRESOLVED` 는 **구조적 OPEN 으로 유지**하고
   25개를 소급 수정하지 않았다.
⛔ RULE-0002 · 0018 의 「DRAM ASP 하락 전환」 동일 문면은 **건드리지 않았다.**

---

## 0. ★★ 먼저 내 직전 보고를 두 군데 정정한다

### 정정 1 — 「RULE-0022 의 정의·임계·측정 대상은 정본에 없다」는 **틀렸다**

정본에 **이미 있다.** 2026-08-15 CIO 판정 3장이 `rules/decision_cards.json` 과
`config/rules.json` 에 원문 그대로 들어 있고, `definition_status` 도 그 판정을
적용해 `UNDEFINED → DEFINED` 로 옮겨져 있다.

내가 본 것은 **정의 적용 이전 상태인 `ssot_mapping.json`** 의
`condition_semantics=UNRESOLVED · missing_components=[threshold, time_window,
comparison_baseline, data_source]` 였다. 적용 후 산출물을 보지 않고 상위 산출물만
읽고 「없다」고 단정했다. 그대로 정정한다.

### 정정 2 — 「0019·0024·0025 가 왜 AVAILABLE 인지 정본에 기록이 없다」도 **틀렸다**

기록이 있다. `rules/build_full_decomposition.py` 에 **선언 시점의 근거가 문장으로**
남아 있다 (§3).

⇒ 두 경우 모두 **「기록이 없다」가 아니라 「내가 다른 산출물을 봤다」**였다.

---

## 1. RULE-0022 의 정의 — 이미 확정돼 있다 (원문 그대로)

```
카드 18  RULE-0022::time_window          CIO 판정 2026-08-15
  「기간 조건은 비교 가능한 증가율이 2 개 연속 공식 보고기간에서 급둔화 조건을
    충족할 때로 한다. 첫 번째 관측만으로는 급둔화를 확정하지 않는다.」
  · 공식 보고기간 기준으로 센다. 달력일로 환산하지 않는다.
  · 비교 가능한 관측이 만들어지지 않은 기간은 횟수를 증가시키지 않는다.
  · 중간에 미성립이면 연속성은 끊기고 다시 첫 관측부터 센다.

카드 19  RULE-0022::comparison_baseline
  「비교 기준은 **동일한 측정 정의에서 산출된 전년동기 성장률**로 한다.
    잔액 자체의 증감이나 직전 보고기간 성장률을 비교 기준으로 사용하지 않는다.
    측정 정의 또는 포함 범위 변경으로 직접 비교할 수 없는 경우 다른 값으로
    보정하지 않고 **판정 불가**로 둔다.
    단기·장기 구성 및 인식기간 변화는 이 비교 기준에 포함하지 않는다.」

카드 20  RULE-0022::threshold
  「「급」 은 전년동기 성장률 대비 **10%p 이상 하락**으로 정의한다.
    ★ 10% 가 아니라 10%p. 정확히 10%p 하락도 포함한다.
    카드 18 의 연속 관측 요건을 함께 충족해야 발동한다.」
  · 계열 정의가 바뀌어 비교 가능하지 않으면 임의 보정하지 않고 평가 불가로 둔다.
  · 수집기 부재는 해결되지 않는다. 정의 세 칸이 채워져도 평가 가능하다는 뜻이 아니다.
```

⇒ **CIO 가 이번에 요구한 「급둔화를 어떤 observable 로 정의할 것인가」는
  이미 닫혀 있다.** ⛔ 그래서 나는 새 선택지를 제시하지 않는다 — 이미 정해진 것에
  대해 선택지를 만드는 것이 정확히 이 프로젝트가 금지해 온 행위다.

### 1-1. CIO 가 제시한 4분류를 확정 정의에 대조

| | CIO 분류 | 확정 정의의 처리 |
|---|---|---|
| ① | 잔액($) | **명시적 배제** — 「잔액 자체의 증감을 비교 기준으로 사용하지 않는다」 |
| ② | YoY growth | **채택** — 단, 「동일한 측정 정의에서 산출된 **전년동기 성장률**」 대비 |
| ③ | next-12-month recognition | **명시적 배제** — 「단기·장기 구성 및 인식기간 변화는 포함하지 않는다」 |
| ④ | 대형 계약 영향 / 회사 조정 설명 | **카드가 이름으로 다루지 않는다** → §2 의 열린 질문 |

★ ②의 비교 기준은 **직전 분기가 아니라 전년동기**다. 즉 `110 → 99 → 84` 라는
  **직전 분기 대비 하락 수열은 이 규칙의 비교 기준이 아니다.**
  CIO 가 우려한 「110 → 99 → 84 니까 급둔화」라는 기계적 판정은 **확정 정의가 이미
  구조적으로 배제하고 있다.**

## 2. ★ 확정 정의에 남아 있는 열린 질문 두 가지 (⛔ 답을 적지 않는다)

### 열린 질문 A — 「측정 대상 계열」이 어느 카드에도 지목돼 있지 않다

```
카드 19  "동일한 **측정 정의**에서 산출된"  ← 측정 정의가 이미 있다고 전제한다
그런데   RULE-0022 에 측정 대상(계열) 카드가 **없다**
         mapping 의 missing_components = [threshold, time_window,
                                          comparison_baseline, data_source]
         → metric identity 항목이 처음부터 목록에 없다
```

우리 fixture 의 행 문면은 **`Commercial remaining performance obligation`** 이다.
CIO 교차검증에 따르면 Microsoft 는 **Commercial RPO** 규모를 별도로 공표한다.
⇒ 「RPO」가 total RPO 인지 Commercial RPO 인지 **정본이 지목한 적이 없다.**

★ 선례가 있다 — `RULE-0001` 은 정확히 이 사유(`metric_identity_missing`)로
  `DEFINED → UNDEFINED` **재개방**됐다 (「`FQ4 $49B` 는 무엇의 $49B 인가?」).
⛔ RULE-0022 를 재개방해야 한다고 적지 않는다. 선례와 사실만 올린다. 판정 사항이다.

### 열린 질문 B — 대형 계약 기저효과가 「판정 불가」 사유인가

카드 19 는 **「측정 정의 또는 포함 범위 변경」** 으로 직접 비교할 수 없을 때
판정 불가로 두라고 한다. 그런데 대형 계약 편입은

```
측정 정의가 바뀐 것이 아니다      — 같은 계열을 같은 방식으로 계속 공표한다
포함 범위가 바뀐 것도 아니다      — 계약 하나가 그 계열에 들어온 것이다
```

⇒ **문면 그대로 읽으면 기저효과는 「판정 불가」 사유에 해당하지 않는다.**
  즉 회사가 별도 조정 설명(예: 특정 대형 계약 제외 기준)을 제공하더라도, 확정 정의는
  그 조정치를 쓰라고 말하지 않는다 — 오히려 카드 19 는 **다른 값으로 보정하지 말라**고 한다.

⛔ 이것이 의도된 결과인지, 카드 작성 시점에 고려되지 않은 경우인지 나는 판정하지 않는다.
   ★ CIO 가 이번에 제기한 우려(기저효과·대형 계약을 수요 급둔화로 오판)는 **비교 기준이
     전년동기라는 점 때문에 상당 부분 이미 막혀 있으나**, 「전년동기 성장률 자체가
     대형 계약으로 부풀려진 경우」는 카드가 다루지 않는다.

## 3. ★★ 실제 blocker 는 정의가 아니라 **전년동기 계열의 부재**다

확정 정의를 우리가 가진 관측치에 그대로 적용해 봤다 (⛔ 판정하지 않는다 · 계산만 한다).

### 우리 artifact 가 실제로 담고 있는 것

```
fixture 4건 (RULE-0021 이 쓰는 바로 그 표)
  2025-10-29  FY26 Q1  Commercial remaining performance obligation   51%  0%   51%
  2026-01-28  FY26 Q2                                               110%  0%  110%
  2026-04-29  FY26 Q3                                                99%  0%   99%
  2026-07-29  FY26 Q4                                                84%  0%   84%
                                                                   GAAP  cc영향  cc
```

관측 사실 셋:

```
1  행 문면이 4건 전부 **동일**하다 — Azure 행처럼 문면이 바뀐 적이 없다
2  cc impact 가 4건 전부 **0%** 다 → GAAP 성장률 == cc 성장률.
   Azure 와 달리 이 계열에는 **cc 컬럼 선택 문제가 없다**
3  슬라이스 안에 `$` · `billion` · `next twelve` · `recognized` · `OpenAI` 는 **0회**.
   즉 ①잔액 ③인식기간 ④조정설명은 **우리 캡처 범위 안에 없다.**
   ⛔ 슬라이스에 없다는 것이 문서에 없다는 뜻은 아니다 — 우리 캡처는 표 하나로 한정된다.
```

### 확정 정의를 적용하면

```
카드 19 가 요구하는 것   현재 보고기간 성장률  vs  **전년동기 성장률**
우리가 가진 것           FY26 Q1 · Q2 · Q3 · Q4 (4개 분기, 전부 같은 회계연도)
전년동기 성장률(FY25 Q1~Q4)  **0건 보유**

⇒ 비교 가능한 관측 쌍 = **0건**
⇒ 카드 18 의 「2개 연속」은커녕 **1회 판정도 성립하지 않는다**
```

★ 그래서 `data_status = MISSING` 의 의미가 이번 조사로 **좁혀진다.**

```
이전 이해   「해당 지표를 읽는 수집기가 없다」
            (decision card 의 atlas_artifact evidence 원문 — 이제 **낡았다**)
실제        수집기는 있다. RULE-0021 이 쓰는 collector·표·행이 그대로 쓰인다.
            없는 것은 **전년동기 계열**, 즉 FY25 분기 8-K 관측이다.
```

⇒ RULE-0022 의 blocker 는 **취득 인프라가 아니라 관측 이력(history) 확보**다.
⛔ 몇 분기를 더 확보해야 하는지, 과거 8-K 를 소급 수집할지는 **판정 사항**이다.
   ⛔ 나는 소급 수집을 시작하지 않았고 sec.gov 에 접근하지도 않았다.

## 4. READY provenance audit — 0019 · 0024 · 0025

### 4-1. 근거를 찾았다 (「근거 없음」이 아니다)

```
rules/build_full_decomposition.py
  268:  R("SK 대비 상대강도 지속 열위", ..., "AVAILABLE", "SUPPORTED", None,
        "'지속'의 정의 없음. **두 한국 종목 종가는 krx.py 로 확보돼 데이터는 있다
         — 정의만 없다.**")
  327:  R("7월 저점 종가 1,894,000원 재이탈", ..., "AVAILABLE", "SUPPORTED", None,
        "★ 46칸 전체에서 evaluator_status 가 READY 로 파생되는 유일한 조각. " + KR_PRICE)
  330:  R("기관 순매수 연속 끊김", ..., "AVAILABLE", "SUPPORTED", None,
        "★ '연속'의 정의가 없다. 데이터는 있고 정의가 없다.")

KR_PRICE = "krx.py v4.1 확정 종가·수급으로 평가 가능"
```

전 파이프라인에서 `data_capability == "SUPPORTED"` 인 rule 은 **이 3건뿐**이다
(`UNRESOLVED 16 · NOT_IMPLEMENTED 6 · SUPPORTED 3`).

### 4-2. 그 근거가 실재하는지 확인했다

```
collectors/krx.py                    존재 (16 KB)
data/2026-08-13|14|15/krx.json       존재 · collector_version "v4.1"
                                     source "KRX 정보데이터시스템 (pykrx)" · tier Official
                                     005930 · 000660 · 298040 세 종목 모두 포함
```

### 4-3. 그래서 무엇이 다른가 — **경로가 다르다**

| | RULE-0003 · 0007 · 0008 · 0021 | RULE-0019 · 0024 · 0025 |
|---|---|---|
| 출발 상태 | `MISSING` | **처음부터 `AVAILABLE`** |
| 승격 경로 | `data_capability_application` (감사 가능) | **없음** (`null`) |
| source_qualification | `SOURCE_UNRESOLVED → SOURCE_RESOLVED` | **한 번도 부여된 적 없음** |
| per-rule 취득 계약 | `DATA_CAPABILITY_SOURCE_BY_RULE` 에 있음 | **없음** |
| 승인 회귀 | collector 회귀가 `run_all.py` 승인 목록에 있음 | **krx 경로를 다루는 승인 회귀 없음** |

⇒ **「근거 없음」이 아니라 「다른 층에서, 다른 형식으로, 검증 없이 선언됨」이다.**
  근거는 문장이고, 그 문장이 가리키는 파일은 실재하며, **그 파일이 실제로 그 Rule 을
  평가할 수 있는지는 회귀로 확인된 적이 없다.**

⛔ 강등·재검증·krx 회귀 신설 중 어느 것도 하지 않았다. 전부 판정 사항이다.

## 5. 판정에 필요한 사실만

1. **RULE-0022 의 정의는 이미 확정돼 있다** (카드 18·19·20, 2026-08-15).
   내 직전 보고의 「정의가 없다」는 오류였다 — 정정한다.
2. 확정 정의는 ①잔액과 ③인식기간을 **명시적으로 배제**하고 ②성장률을 채택하되
   비교 기준을 **전년동기 성장률**로 못박았다. 따라서 `110→99→84` 같은
   **직전 분기 수열 판정은 이미 구조적으로 배제돼 있다.**
3. 열린 질문 A — **측정 대상 계열(Commercial RPO vs total RPO)을 지목한 카드가 없다.**
   RULE-0001 이 같은 사유로 재개방된 선례가 있다.
4. 열린 질문 B — 대형 계약 기저효과는 문면상 「측정 정의·포함 범위 변경」이 아니므로
   **「판정 불가」 사유에 해당하지 않는다.** 회사 조정치를 쓰라는 근거도 카드에 없다.
5. **실제 blocker 는 취득 인프라가 아니라 전년동기 관측의 부재다.**
   현재 4건은 전부 FY26 이라 **비교 가능한 쌍이 0건**이다.
6. RULE-0022 계열은 cc impact 가 4건 전부 0% 라 **cc 컬럼 선택 문제가 없다** (Azure 와 다름).
7. 0019·0024·0025 의 AVAILABLE 근거는 **있다** — `krx.py v4.1`. 파일과 데이터도 실재한다.
   다만 취득 계약·source_qualification·승인 회귀를 **거치지 않았다.**

⛔ 권고를 적지 않는다.

## 6. 하지 않은 것

- Rule 상태 변경 · 정의 신설 · 임계 신설 · 재개방 · 강등
- 과거 8-K 소급 수집 · sec.gov 접근 · 외부 수치의 파이프라인 편입
  (CIO 교차검증 수치 $392B/$625B/$627B · ex-OpenAI 26% 는 **CIO 제공**으로만 기록한다.
   우리 artifact 는 성장률만 담고 있어 이 값들을 검증하지 못한다)
- `condition_semantics/scope 25/25` 소급 작업
- RULE-0002 · 0018 동일 문면 처리
- krx 회귀 신설 · 0019·0024·0025 강등 또는 재검증
- 별건 1~3 · 5~7 착수 · CI 5th gate 변경

## 7. 상태

```
RULE-0022
  definition            DEFINED @ CIO 판정 2026-08-15 (카드 18·19·20) — 이미 확정
  measurement identity  ★ 계열 지목 카드 없음 — 판정 대기 (RULE-0001 선례 있음)
  data                  MISSING — blocker 는 수집기가 아니라 **전년동기 관측 부재**
  source_qualification  없음 · data_capability NOT_IMPLEMENTED (axis: data_source)
  evaluator             BLOCKED

READY provenance audit
  0003·0007·0008·0021   취득 계약 통과 · 승인 회귀 있음
  0019·0024·0025        근거 있음(krx.py v4.1) · 계약·source_qual·승인 회귀 없음 — 판정 대기
  READY 해석            **Evaluator READY** 로 한정 (CIO 판정 2026-08-16)

구조적 OPEN
  condition_semantics/scope 25/25 UNRESOLVED — 소급 작업 미승인 · 유지
  RULE-0002 · 0018 동일 문면 — 「하나의 시장 Fact → 여러 종목 Rule」 일반화 후보 · HOLD

별건 1·2·3·5·6·7        OPEN · 미착수
mutation infrastructure  CLOSED @ 11ee80d · evidence AUTHORITATIVE @ 350948b
CI 5th gate              NOT APPROVED
```

```
UNDEFINED 3 · MISSING 18 · SOURCE_UNRESOLVED 12 · BLOCKED 18 · READY 7
consumable_by_evaluator = false · Production HOLD
```
