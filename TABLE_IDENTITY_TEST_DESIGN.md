# C4 final observation candidate uniqueness — 회귀/FI 설계안 (2026-08-16)

⛔ **설계만이다. 코드 변경 0줄 · 새 합성 FI 미실행 · 수정 미착수.**
⛔ P3 · `RULE-0003/0007/0008` 불변.

확정된 계약 (CIO 판정 2026-08-16):

```
L1 candidates → 각 후보에 L2 적용 → L1∧L2 성공 후보 **전체** 수집
              → 정확히 1건이면 선택 / 0건·2+ 면 evidence 와 함께 fail-closed
```

---

## 1. ★ 먼저 보고할 구조적 제약 — 순수함수가 없다

현재 selection 은 `main()` 안에 인라인이다.

```python
for ti, rows, di in found:            # ← L1 후보
    header = build_header(rows, di)
    bound, probs = bind_columns(header, rows[di], month_name, ty)
    if bound:
        decision = bound
        break                          # ← 첫 성공에서 멈춘다 (계수하지 않는다)
    why.append(...)
```

`main()` 은 네트워크·출력·종료코드를 함께 갖는다. **테스트가 호출할 수 있는 단위가
아니다.** 따라서 아래 A·B·C 중 무엇을 택하든 **순수함수 추출이 선행**되어야 한다.

⇒ 그 추출 자체가 코드 변경이므로 **별도 승인 대상**이다. 여기서는 하지 않았다.

제안 형태 (⛔ 미구현):

```python
def final_candidates(tables, month_name, year, rejected=None) -> list:
    """L1∧L2 를 모두 통과한 표를 **전부** 돌려준다. ⛔ 고르지 않는다."""
```

`select_observation`(MSFT)과 같은 역할이며, 고르는 판단은 호출부가 유일성으로 한다.

## 2. 검증 방법 세 가지 — 무엇을 증명하고 무엇을 못 하는가

| | 방법 | 2+ 를 만들 입력이 필요한가 | 증명하는 것 | 못 하는 것 |
|---|---|---|---|---|
| **A** | 정적(AST) — selection 함수에 `break` 없음 · 반환이 전량 수집 | ❌ 불필요 | 「첫 성공에서 멈추지 않는다」는 **구조** | 실제 계수·거부 동작 |
| **B** | 순수함수 단위 — **파싱된 표 리스트를 복제**해 `[t4, t4]` 로 호출 | ⚠️ 자료구조 복제 (markup 합성 아님) | 2+ 에서 **fail-closed 하고 evidence 를 남긴다** | 실제 문서가 그렇다는 것 |
| **C** | 실제 fixture positive — 3개월에서 `L1∧L2 == 1` | ❌ 불필요 | 정상 원문에서 **관측이 그대로 유지**된다 | 2+ 경계 |

★ **A + B + C 를 함께 써야 계약이 닫힌다.** 하나만으로는 부족하다.
  - A 만: 「구조가 그렇다」만 남고 실제 거부를 안 본다
  - C 만: 정상 경로만 본다 — 지금도 통과하므로 **아무것도 새로 잡지 못한다**
  - B 만: 계수는 보지만 구조 회귀(다시 `break` 넣기)를 놓칠 수 있다

## 3. ★ B 의 성격 — CIO 판정이 필요한 지점

CIO 지시: 「실제 입력으로 2+를 만들려면 결국 결정표 수준 합성이 필요하므로,
그 필요성이 확인된 뒤 별도 승인하겠습니다.」

**확인 결과: markup 합성 없이도 2+ 를 만들 수 있다.** 단, 층이 다르다.

```python
# markup 합성 (❌ 미승인)          결정표 HTML 을 복제·편집해 문서를 만든다
# 자료구조 복제 (❓ 판정 필요)      실제 fixture 를 파싱한 결과를 리스트로 두 번 넘긴다
p = TableCollector(); p.feed(real_t4_fixture)
final_candidates(p.tables + p.tables, "July", 2026)   # ← 같은 실제 표 2개
```

- **주장하는 것**: 「후보가 2건일 때 이 함수가 어떻게 행동하는가」 — 계수 로직의 단위 시험
- **주장하지 않는 것**: 「실제 TSMC 문서에 그런 구조가 있다」 — ⛔ 절대 아니다
- markup 을 만들지 않고 **실제 원문 표를 그대로** 쓴다는 점에서 앞선 합성 FI 와 다르다

⇒ 이것을 「합성」으로 볼지 「단위 시험」으로 볼지는 **CIO 판정 사항**이다.
  ⛔ 내가 정하지 않고, 승인 전에는 실행하지 않는다.

  · **합성으로 본다면** → A + C 만으로 가고, 2+ 경계는 미검증으로 명시 기록
  · **단위 시험으로 본다면** → A + B + C 로 계약이 완전히 닫힌다

## 4. 회귀 설계 (승인 후 구현)

### C — positive · 실제 fixture 3개월

```
T-1  각 월 t4 에서 L1 후보 1건
T-2  그 후보가 L2 통과 → final_candidates == 1건
T-3  값이 P3 기록과 동일 (June 442,680/67.9/2,404,484/35.6 · July 467,580/44.7/…)
T-4  ★ t6(천원표)은 최종 후보에 들어오지 않는다 — L1 에서 두 이유로 각각 걸린다
T-5  ★ 최소변형(t6 에 Y-o-Y 부여)에서도 최종 후보는 여전히 1건
     ⇒ 「가」를 채택하지 않은 판정이 회귀로 고정된다 (L2 정상 탈락을 버리지 않는다)
```

### A — 정적 · 구조

```
T-6  selection 함수에 `break` 제어문이 없다 (AST · ⛔ 문자열 검색 아님)
T-7  main 이 그 함수를 쓰고, main 안에서 직접 고르지 않는다
T-8  거부 evidence 통로(rejected)가 있다
T-9  ⛔ 범위 밖 미변경 — L3 는 여전히 문서 단위 · build_header 미변경
```

### B — 단위 · 2+ 경계 (⚠️ 3절 판정 후에만)

```
T-10  같은 실제 표 2개 → final_candidates 2건 → 호출부가 fail-closed
T-11  evidence 에 표 번호 전부가 남는다 (첫 번째만이 아니라)
T-12  0건 → fail-closed · 사유 구분
T-13  순서를 바꿔도 같은 결과(거부) — 배치가 값을 정하지 않는다
```

### 변이 계획 (판별력 확인)

```
V-1  break 를 되살린다            → T-6 · T-10 이 잡아야 한다
V-2  `!= 1` 을 `< 1` 로           → T-10 이 잡아야 한다
V-3  L2 실패 후보도 최종에 넣는다   → T-5 가 잡아야 한다 (「가」 회귀 방지)
V-4  evidence 를 첫 건만 남긴다     → T-11 이 잡아야 한다
```

⚠️ **B 가 빠지면 V-1·V-2·V-4 를 잡을 수 없다.** A 의 정적 검사가 V-1 은 잡지만
V-2·V-4 는 못 잡는다. 이 한계를 미리 기록해 둔다 — 나중에 「회귀가 있으니 안전하다」로
읽히지 않게 하기 위해서다.

## 5. 판정을 기다리는 것

1. **순수함수 추출 승인 여부** — 이것 없이는 A·B 어느 것도 불가능하다
2. **B 의 성격 판정** — 자료구조 복제를 단위 시험으로 볼 것인가, 합성으로 볼 것인가
3. B 를 배제할 경우, 2+ 경계를 **미검증 영역으로 명시 기록**하는 데 동의하는지
   (FI-3 · D-4~D-7 과 같은 취급)

⛔ 권고를 적지 않는다.

## 6. 상태

```
C4 row-local uniqueness          CLOSED @ 36aa11b
C4 table-level ambiguity         OPEN — contract resolved, implementation gap under test
build_header robustness          OPEN — 후순위 (selection 계층 종료 후)
RULE-0003 · 0007 · 0008          P3 CLOSED 유지 · READY
```
